# Theme background textures

Place these files here (used by theme system; URL path in app: `/textures/...`):

- `forest-wide.png` — full-cover background texture
- `forest-tile.png` — tileable texture (e.g. 700px repeat)
